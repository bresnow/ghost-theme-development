{
  "name": "theme-one",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Theme One — Ghost theme with blue/teal palette",
  "author": {
    "name": "Example Author",
    "email": "hello@example.com"
  },
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "lint": "npx gscan ."
  },
  "keywords": [
    "ghost-theme"
  ],
  "engines": {
    "ghost": ">=6.0.0"
  },
  "config": {
    "posts_per_page": 12
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "postcss": "^8.0.0",
    "autoprefixer": "^10.0.0"
  }
}
